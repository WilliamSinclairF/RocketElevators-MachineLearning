<div id="header" class="navbar-toggleable-md sticky header-md clearfix">
  <!-- TOP NAV -->
  <header id="topNav">
    <div class="container">
      <!-- Mobile Menu Button -->
      <button class="btn btn-mobile" data-toggle="collapse" data-target=".nav-main-collapse">
        <i class="fa fa-bars"></i>
      </button>
      <!-- Logo -->
      <a class="logo float-left scrollTo" href="#top">
        <%= image_tag('logo/R2.png') %>
      </a>
      <div class="navbar-collapse collapse float-right nav-main-collapse">
        <nav class="nav-main">
          <ul id="topMain" class="nav nav-pills nav-main nav-onepage">
            <li class="active">
              <%= link_to 'HOME', root_path %>
            </li>
            <li class="dropdown nav-animate-fadeIn resp-active"><!-- SERVICES -->
              <%= link_to 'SERVICES', root_path(anchor: "services") %>
              <ul class="dropdown-menu">
                <li class="dropdown">
                  <a href="/static_pages/residential">RESIDENTIAL SERVICES</a>
                </li>
                <li class="dropdown">
                  <a href="/static_pages/corporate">CORPORATE SERVICES</a>
                </li>
                <li class="dropdown">
                  <%= link_to 'GET A QUOTE', new_quote_path %>
                </li>
              </ul>
            </li>
            <li class="dropdown nav-animate-fadeIn resp-active"><!-- SERVICES -->
              <%= link_to 'ABOUT US', root_path(anchor: "portfolio") %>
              <ul class="dropdown-menu">
                <li class="dropdown">
                  <%= link_to 'PORTFOLIO', root_path(anchor: "portfolio") %>
                </li>
                <li class="dropdown">
                  <%= link_to 'INDUSTRY NEWS', root_path(anchor: "news") %>
                </li>
                <li class="dropdown">
                  <%= link_to 'OUR CLIENTS', root_path(anchor: "clients") %>
                </li>
              </ul>
            </li>
            <li><!-- CONTACT -->
              <%= link_to 'CONTACT', root_path(anchor: "contact")%>
            </li>
            <% if user_signed_in? %>
              <!-- ACCOUNT DROPDOWN -->
              <li class="dropdown nav-animate-fadeIn resp-active">
                <% if current_user.is_admin? || current_user.is_employee? %>
                  <%= link_to 'EMPLOYEE SECTION', edit_user_registration_path %>
                <% else %>
                  <%= link_to 'MY ACCOUNT', edit_user_registration_path %>
                <% end  %>
                <ul class="dropdown-menu">
                  <li class="dropdown">
                    <%= link_to 'MY PROFILE', edit_user_registration_path %>
                  </li>
                  <% if current_user.is_employee? || current_user.is_admin? %>
                    <li>
                      <%= link_to 'INTERVENTIONS', interventions_path %>
                    </li>
                    <li><!-- BACKOFFICE -->
                      <%= link_to 'BACKOFFICE', rails_admin_path %>
                    </li>
                  <% end %>
                  <% unless current_user.is_employee? || current_user.is_admin? %>
                    <li class="dropdown">
                      <%= link_to 'MY QUOTES', my_quotes_path %>
                    </li>
                    <li class="dropdown">
                      <%= link_to 'MY LEADS', my_leads_path %>
                    </li>
                  <% end %>
                  <li class="dropdown">
                    <%= link_to 'SIGN OUT', destroy_user_session_path, method: :delete %>
                  </li>
                </li>
              <% else %>
                <li><!-- REGISTER -->
                  <%= link_to 'REGISTER', new_user_registration_path %>
                </li>
                <li><!-- SIGN IN -->
                  <%= link_to 'SIGN IN', new_user_session_path %>
                </li>
              <% end %>
            </ul>
          </nav>
        </div>
      </div>
    </header>
    <!-- /Top Nav -->
  </div>
